{
  "name": "your-id",
  "version": "1.0.0",
  "description": "Id management system",
  "workspaces": [
    "apps/**/*"
  ],
  "scripts": {
    "start:dev": "concurrently \"npm run start:dev --workspace=your-id-web\" \"npm run start:dev --workspace=your-id-service\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
